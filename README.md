# Instagram-User-Clustering-
Designed a scalable clustering framework using subsampling techniques to handle large datasets efficiently while maintaining statistical validity. Combined macro segmentation (K-Means) with micro behavioral segmentation (hierarchical clustering) to derive meaningful user engagement personas.
